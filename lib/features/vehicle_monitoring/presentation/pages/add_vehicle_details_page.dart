import 'package:flutter/material.dart';

class AddVehicleDetailsPage extends StatelessWidget {
  const AddVehicleDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
